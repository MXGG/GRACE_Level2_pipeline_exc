"""Compatibility layer package.

This package is reserved for import-path shims during the refactor window.
New code must depend on canonical modules under ``app/``, ``domain/``,
``infra/`` and ``ui/`` instead of compat wrappers.
"""
