# GRACE Pipeline core CLI dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
netCDF4>=1.5.0
h5py>=3.0.0
click>=8.0.0
tqdm>=4.60.0
pyyaml>=6.0
joblib>=1.1.0
pyshp>=2.1.0

# Optional GUI dependency; install only for desktop GUI usage:
#   python -m pip install -e ".[gui]"
# PySide6>=6.7.0

# Optional scientific extensions used by some experiments:
# pyshtools>=4.10.0

# Build dependencies are declared in pyproject.toml under [project.optional-dependencies].
